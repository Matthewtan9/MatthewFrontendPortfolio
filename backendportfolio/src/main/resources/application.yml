# Default Spring Profile
server:
  error:
    include-message: always

logging:
  level:
    root: INFO
    com.example: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

---


# for docker

spring:
  datasource:
    url: jdbc:mysql://mysql1/portfolio-db
    username: user
    password: pwd

  jpa:
    hibernate:
      ddl-auto: none

  config:
    activate:
      on-profile: docker

  sql:
    init:
      platform: mysql
      mode: always

#  mail:
#    host: smtp.gmail.com
#    username: matthew999099@gmail.com
#    password: ocvzrjmtvwqhtqtm
#    port: 587
#    properties:
#      mail:
#        smtp:
#          auth: true
#          starttls.enable: true

server.port: 8080

spring.datasource.hikari.initializationFailTimeout: 60000